## 1.查看空间占用情况
du -h --max-depth=1 /home/wangmin | sort -hr | head -20
du -h --max-depth=1 ~/anaconda3 | sort -hr
## 2.清理pip缓存
pip cache purge

## 3.清理conda缓存
conda clean -a -y

## 4.删除conda无用环境
conda remove -n “name” --all -y

## 5.清理apt环境
sudo apt clean
sudo apt autoremove -y

## 6.清理Huggingface模型缓存
rm -rf ~/.cache/huggingface

## 7. pip临时构建缓存
rm -rf ~/.cache/pip

## 8.torch/CUDA	缓存
rm -rf ~/.cache/torch

## 9.vLLM/Triton编译缓存
rm -rf ~/.cache/triton

## 10 .docker无用镜像
docker system prune -a -f

## 11.关闭wsl后压缩空间
Powershell:
a.  wsl --shutdown

b.  Optimize-VHD -Path "C:\Users\wangmin\AppData\Local\Packages\CanonicalGroupLimited.Ubuntu20.04LTS_79rhkp1fndgsc\LocalState\ext4.vhdx" -Mode Full
