conda install mamba -n base -c conda-forge -y

mamba可以替换conda
mamba env create -f ml.yml  混合环境
mamba env create -f cv.yml
mamba env create -f audio.yml
mamba env create -f nlp.yml

后续如果要更新yml,
mamba env list   查看当前环境列表
新建环境      mamba env create -f ml.yml 
全量更新环境   mamba env update -n ml -f ml.yml --prune 
删除环境和包   mamba env remove -n ml
清理全局的缓存 conda clean --all -y 

===================================
后端运行多个环境python代码脚本；或者使用docker运行多个；注意会抢GPU要提前分配
#!/bin/bash

conda run -n cv python vision_api.py &
conda run -n nlp python llm_api.py &
conda run -n audio python speech_api.py &

wait