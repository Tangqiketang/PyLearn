name: audio
channels:
  - conda-forge
dependencies:
  - python=3.10
  - ffmpeg
  - numpy
  - pandas
  - pip
  - pip:
      - --pre
      - torch
      - torchaudio
      - --extra-index-url https://download.pytorch.org/whl/nightly/cu128
      - librosa
      - soundfile
      - openai-whisper
      - pyttsx3
      - gradio
      - protobuf
      - huggingface_hub
