监督学习（有目标值）：
    分类(目标离散值)：
        k近邻、
        朴素贝叶斯分类
        决策树与随机森林、随机森林最常用
        逻辑回归、
        神经网络
    回归（目标连续值）没有准确率只有偏差：
        线性回归、
        岭回归
    标注：
        隐马尔可夫模型
非监督学习（无目标值）：
    聚类 k-means

集成学习方法：
    多个分类器/模型，独立学习预测。这些预测最后结合成单预测，这样就优于单一的分类器。
随机森林最常用
================================================
流程:
    1.数据基本处理：pandas处理数据缺失、合并表、one-hot、分割测试集训练集、看是否需要标准化（独立的两个标准化）
    2.特征工程：
        选择哪种
            分类
            回归
            聚类
    3.找到合适的算法进行预测
    4.模型的评估，判定效果==》没有合格换算法和参数
    5.上线以API方式提供


归一化：特征除以最大值，会受异常点影响
标准化：解决跨量冈特征.数据压缩到正太分布

欠拟合： 训练集结果不太行
过拟合： 模型太复杂，模型尝试兼顾所有的点导致。
        1.交叉验证（只能检验不能解决）
        2.特征选择（很难看出来）
            过滤式：
                低方差特征
            嵌入式：
                正则化
                决策树
                NN
        3.正则化
            把高次项特征的权重降到靠近0,曲线就会变平一点
 ==================================
 数据切分from sklearn.model_selection import train_test_split
     x_train,x_test,y_train,y_test = train_test_split(news.data,news.target,test_size=0.25)
 特征提取
    from sklearn.feature_extraction.text import TfidfVectorizer 特征提取

